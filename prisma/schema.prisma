// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

enum SetlistStatus {
  ONGOING
  UPCOMING
  COMPLETED
}

model Concert {
  id BigInt @id @default(autoincrement())
  code String @db.VarChar(20) @unique
  title String @db.VarChar(100)
  startDate String @db.VarChar(20) @map("start_date")
  endDate String @db.VarChar(20) @map("end_date")
  status SetlistStatus @default(ONGOING)
  daysLeft BigInt? @map("days_left")
  poster String? @db.VarChar(100)
  artist String? @db.VarChar(100) 
  createdAt DateTime @map("created_at") @default(now())
  updatedAt DateTime @map("updated_at") @updatedAt
  cultures Culture[]
  setlists Setlist[]

  @@fulltext([title, artist])

  @@map("concerts")
}

model Culture {
  id BigInt @id @default(autoincrement())
  concertId BigInt @map("concert_id")
  content String? @db.VarChar(100)
  imgUrl String? @db.VarChar(100) @map("img_url")
  createdAt DateTime @map("created_at") @default(now())
  updatedAt DateTime @map("updated_at") @updatedAt

  concert Concert @relation(fields: [concertId], references: [id])

  @@index([concertId])
  
  @@map("cultures")
}

enum SetlistType {
  ONGOING
  PAST
  EXPECTED
}
model Setlist {
  id BigInt @id @default(autoincrement())
  concertId BigInt @map("concert_id")
  title String @db.VarChar(50)
  type SetlistType @default(ONGOING)
  status String @db.VarChar(10)
  date DateTime?
  imgUrl String? @db.VarChar(100)
  artist String? @db.VarChar(100)
  createdAt DateTime @map("created_at") @default(now())
  updatedAt DateTime @map("updated_at") @updatedAt
  
  setlistSongs SetlistSong []

  concert Concert @relation(fields: [concertId], references: [id])

  @@index([concertId])

  @@map("setlists")
}

model SetlistSong {
  id BigInt @id @default(autoincrement())
  setlistId BigInt @map("setlist_id")
  songId BigInt @map("song_id")
  order BigInt @map("order_index")
  fanchant String? @db.Text


  setlist Setlist @relation(fields: [setlistId],references: [id])
  song Song @relation(fields: [songId],references:[id])

  @@index([setlistId, songId])
  
  @@map("setlist_songs")
}

model Song{
  id BigInt @id @default(autoincrement())
  title String @db.VarChar(50) 
  artist String @db.VarChar(50)
  imgUrl String? @db.VarChar(100) @map("img_url")
  lyrics String? @db.Text
  pronunciation String? @db.Text
  translation String? @db.Text
  createdAt DateTime @map("created_at") @default(now())
  updatedAt DateTime @map("updated_at") @updatedAt
  
  setlistSongs SetlistSong []
  @@map("songs")
}

model Banner {
  id BigInt @id @default(autoincrement())
  imgUrl String? @db.VarChar(100) @map("img_url")
  category String? @db.VarChar(30)
  title String? @db.VarChar(50)
  createdAt DateTime @map("created_at") @default(now())
  updatedAt DateTime @map("updated_at") @updatedAt

  @@map("banners")
}